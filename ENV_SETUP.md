# إعداد متغيرات البيئة

هذا المشروع يتطلب متغيرات البيئة التالية ليعمل بشكل صحيح:

## متغيرات البيئة المطلوبة

### Gemini API (للتفريغ والتصنيف)
```
GEMINI_API_KEY="your_gemini_api_key_here"
```

للحصول على مفتاح Gemini المجاني:
1. اذهب إلى [aistudio.google.com](https://aistudio.google.com)
2. أنشئ حساباً بحساب Google
3. اضغط على "Get API key" في الشريط الجانبي
4. أضف المفتاح إلى `.env`

**ملاحظة:** Gemini مجاني للاستخدام المعتدل مع نماذج Flash:
- `gemini-2.5-flash`: 10 طلبات/دقيقة، 1500 طلب/يوم
- `gemini-2.5-flash-lite`: 15 طلبات/دقيقة، 1000 طلب/يوم
- `gemini-2.0-flash-lite`: 15 طلبات/دقيقة، 1500 طلب/يوم

النظام يتحول تلقائياً بين النماذج عند وصول الحد اليومي.

### قاعدة البيانات
```
DATABASE_URL="file:./dev.db"
```

### NextAuth
```
AUTH_SECRET="your_nextauth_secret_here"
NEXTAUTH_URL="http://localhost:3000"
```

### متغيرات اختيارية
```
CLASSIFY_TIMEOUT_MS="120000"
```

## إنشاء ملف .env

أنشئ ملف `.env` في جذر المشروع وأضف المتغيرات أعلاه مع قيمك الفعلية.

## ملاحظة

- ملف `.env` مدرج في `.gitignore` ولن يتم إيداعه في المستودع
- لا تشارك مفتاح API الخاص بك مع أي شخص
- استخدم مفاتيح مختلفة للبيئة التطويرية والإنتاجية
- النظام يستخدم نماذج Gemini المجانية مع تحويل تلقائي بين النماذج